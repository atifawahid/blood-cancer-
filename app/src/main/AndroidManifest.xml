<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.atifasec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AtifaSEC">
        <activity
            android:name=".page1"
            android:exported="false" />
        <activity
            android:name=".therapy"
            android:exported="false" />
        <activity android:name=".CancelAppointmentActivity" />
        <activity android:name=".AppointmentScheduling2" />
        <activity android:name=".AppointmentScheduling1Activity" />
        <activity android:name=".AppointmentSchedulingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>